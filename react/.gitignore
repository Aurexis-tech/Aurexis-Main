node_modules/
dist/
*.log
.DS_Store
.vscode/
.idea/
.env*

# Playwright output (browser binaries live in the global cache, not the repo)
test-results/
playwright-report/
.cache/

# Parity screenshots — ~30MB at 7 viewports × 8 stages, regenerable via
# `npm run parity`. The committed evidence is parity/parity-summary.json.
parity/*.png
